export interface UserDetails {
    id: string;
    email: string;
    position: string;
    work_start: string;
    role: string;
    contract: string;
    full_name: string;
    avatar_url?: string;
    password: string;
}